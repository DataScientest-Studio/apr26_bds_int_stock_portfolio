# B_Features — Feature explanation ("Plan B", feature-stages F0–F5)

A **subordinate helper** explaining *what the features are* and where they come from (OHLCV → F0–F5).
**NOT** a build SOT, **decoupled from any data store**. Uses **feature-stages
`F0–F5`** and ids `f{stage}_{metric}…` — never `layer` / `L#` (those belong to the Main_Pipeline in
[`../A_Layers/`](../A_Layers/)).

- Explanation doc: [`feature_explanation_plan_b_eng.md`](feature_explanation_plan_b_eng.md)
- Viz (OHLCV → F0–F5 DAG): [`viz/feature_dag.html`](viz/feature_dag.html)

The build SOT is the Main_Pipeline — see
[`../A_Layers/ENG/build_contract_eng.md`](../A_Layers/ENG/build_contract_eng.md).
